# 环路 / 异常告警 · 检测规则清单（规格书 v1）

> 面向 **V4（阉割版）/ V5（完整版）** 两个独立工具的实现依据。
> 原则：**不解析厂商"高级告警字段"**，而是「标准协议命令 + 两次采样差分 + 关键字映射表」。
> 状态图例：`✔ 已确认` / `⏳ 待实测`（在实验室设备上核对真实输出）

---

## 0. 版本能力边界

| 编号 | 检测项 | V4 阉割 | V5 完整 | 依赖数据 |
|---|---|:---:|:---:|---|
| D1 | MAC 漂移（单设备） | ✔ | ✔ | 单设备 × 2 轮 |
| D2 | STP 拓扑变化（TC 增量） | ✔ | ✔ | 单设备 × 2 轮 |
| D3 | 根桥变更 | ✔ | ✔ | 单设备 × 2 轮 |
| D4 | 广播/组播风暴 | ✔ | ✔ | 单设备 × 2 轮 |
| D5 | 接口错误计数（CRC / input errors） | ✔ | ✔ | 单设备 × 2 轮 |
| D6 | 链路震荡（up/down 次数） | ✔ | ✔ | 单设备 × 2 轮 |
| D7 | 厂商告警关键字映射 | 简化（4 类） | 完整 | 单设备日志/告警 |
| D8 | BPDU 收发异常 | ✗ | ✔ | 单设备 × 2 轮 |
| D9 | **跨设备关联**（MAC 全局冲突 / 同时刻 TC 抖动） | ✗ | ✔ | **≥2 台设备** |
| D10 | **光模块收发功率（光衰）** | ✔ | ✔ | 单设备 × 2 轮 |
| D11 | **温度告警** | ✔ | ✔ | 单设备 × 2 轮 |
| D12 | **电源状态告警** | ✔ | ✔ | 单设备（状态类） |
| — | **在线两轮扫描**（自动重连采样） | ✗ | ✔ | SSH 可达 |
| — | **离线解析**（读巡检输出文件） | ✔ **主用法** | ✔ | 输出文件 |

**V4 定位**：喂进去一份巡检输出 → 出"可疑清单"。不连设备、不做拓扑推理。
**V5 定位**：能连设备跑两轮，也能离线解析；含跨设备关联与 BPDU 分析。

---

## 1. 厂商命令映射表（V5 在线模式用；V4 只认离线文件中的命令名）

| 用途 | 华为 VRP | 华三 Comware | 锐捷 RGOS | 思科 IOS | Linux |
|---|---|---|---|---|---|
| STP 状态 | `display stp`<br>`display stp brief` | `display stp`<br>`display stp brief` | `show spanning-tree`<br>`show spanning-tree summary` | `show spanning-tree detail` | `bridge link` |
| MAC 表 | `display mac-address` | `display mac-address` | `show mac-address-table` | `show mac address-table` | `bridge fdb show` |
| MAC 漂移专用 | `display mac-address flapping record` ⏳ | `display mac-address mac-move` ⏳ | `show mac-address-table flapping` ⏳ | `show mac address-table flapping` ⏳ | — |
| 接口计数 | `display interface` | `display interface` | `show interfaces` | `show interfaces` | `ip -s link` |
| 接口概要 | `display interface brief` | `display interface brief` | `show interfaces status` | `show interfaces status` | `ip -br link` |
| CPU | `display cpu-usage` | `display cpu-usage` | `show cpu` | `show processes cpu sorted` | `top -bn1` |
| 告警/日志 | `display alarm active`<br>`display logbuffer` | `display alarm`<br>`display logbuffer` | `show logging` | `show logging` | `dmesg -T` |
| BPDU | `display stp interface` | `display stp interface` | `show spanning-tree interface` | `show spanning-tree interface detail` | — |
| 光模块 | `display transceiver diagnosis interface` ⏳ | `display transceiver diagnosis interface` ⏳ | `show interfaces transceiver` ⏳ | `show interfaces transceiver detail` ⏳ | `ethtool -m` |
| 温度 | `display temperature all`<br>`display environment` | `display environment` | `show temperature`<br>`show environment` | `show environment temperature` | `sensors` |
| 电源 | `display device`（PWR1/PWR2 状态列） | `display power`<br>`display device` | `show power`<br>`show environment` | `show power`<br>`show env power` | — |

> ⚠️ 各家 frágil 点：命令是否需要 `| include`/分页关闭（华为 `screen-length 0 temporary`、思科 `terminal length 0`）；**解析必须大小写不敏感**（同型号不同版本输出大小写会变）。

---

## 2. 检测规则明细

### D1 · MAC 漂移（置信度：高）
- **判据（单设备）**：两轮采样间，**同一 MAC 在同一设备上从一个端口"跳"到另一个端口**；或 MAC 条目在两轮间反复增删。
- **加强判据**：同一 MAC 的端口归属**在两轮间变化 ≥ 1 次**即报"可疑"，≥ 2 次报"高危"。
- **阈值**：`mac_move_count ≥ 1`（可疑）/ `≥ 2`（高危）
- **误报场景**：终端漫游（无线/笔记本换口）、VLAN 内正常老化 → **缓解**：排除无线 AP 下挂口；对同一 MAC 要求"来回跳"才算高危。
- **V4/V5**：均支持（V5 额外做 D9 跨设备）

### D2 · STP 拓扑变化（TC）增量（置信度：高）
- **判据**：两轮间 `TC count`（华为 `display stp` 的拓扑改变次数 / 思科 `Number of topology changes`）**增量**超阈值。
- **阈值**：接入层 `> 5 /min`；汇聚 `> 15 /min`；核心 `> 30 /min`（可按设备类型覆盖，见 §4）
- **误报场景**：正常终端接入/拔出会引发 TC（尤其边缘端口未配 `edge-port`/`portfast`）→ **缓解**：区分"边缘口 TC"与"主干口 TC"；只看主干口。
- **V4/V5**：均支持

### D3 · 根桥变更（置信度：中）
- **判据**：两轮间**根桥 ID/根端口**发生变化；或 `designated root` 与本地 `bridge ID` 出现非预期组合。
- **阈值**：`root_change ≥ 1` 即报
- **误报场景**：正常主备切换、计划性割接 → **缓解**：结合 D2（TC 同时暴增才升级为高危）。
- **V4/V5**：均支持

### D4 · 广播/组播风暴（置信度：高）
- **判据**：两轮间接口 `broadcast`/`multicast` 包计数**增量 ÷ 间隔** = 速率，超阈值；或广播占接口带宽比例过高。
- **阈值**：`> 1000 pps` 或 `占带宽 > 5%`（可按接口速率换算，见 §4）
- **辅证**：同时段 CPU 占用 > 70%（风暴会拖高 CPU）
- **误报场景**：ARP 扫描、批量 PXE 启动 → **缓解**：结合 D1/D2；只在"无变更窗口"内报。
- **V4/V5**：均支持

### D5 · 接口错误计数（置信度：高）
- **判据**：两轮间 `CRC errors` / `input errors` / `runts` **增量**超阈值（持续增长 = 物理层劣化）。
- **阈值**：`> 100 /min`（可疑）/ `> 1000 /min`（高危）
- **误报场景**：几乎无误报（计数增长就是事实）→ 只是**不能直接等于环路**（也可能光衰/线序/网线质量）
- **V4/V5**：均支持

### D6 · 链路震荡（置信度：高）
- **判据**：两轮间或日志中接口 `up/down` 次数增量超阈值。
- **阈值**：`> 3 /h`（可疑）/ `> 10 /h`（高危）
- **误报场景**：计划性插拔 → **缓解**：结合日志时间戳判断是否人工操作时段。
- **V4/V5**：均支持

### D7 · 厂商告警关键字映射（置信度：中）
- **判据**：日志/告警文本命中 `rules.yaml` 中的厂商关键字 → 映射为统一信号。
- **关键字示例**（**全大小写不敏感**）：

| 统一信号 | 华为 | 华三 | 锐捷 | 思科 |
|---|---|---|---|---|
| `MAC_FLAPPING` | `MFLPVLAN`、`MAC.*flapping` | `MAC_MOVE`、`mac-move` | `MACFLAP`、`FLAPPING` | `MACFLAP_NOTIF`、`MAC.*flapping` |
| `STP_TOPO_CHANGE` | `STP/\d/TC`、`topology.*change` | `STP_TOPO_CHANGE` | `SPANTREE-6` | `SPANTREE-2`、`SPANTREE-6` |
| `BROADCAST_STORM` | `storm.*control`、`broadcast.*suppression` | 同 | 同 | 同 |
| `LOOP_DETECTED` | `loop.*detect`、`loopback.*detect` | 同 | 同 | `%ETHCNTR-3-LOOP_BACK` |

- **误报场景**：关键字出现在历史日志 → **缓解**：只扫"本轮新增"日志段
- **V4**：仅做 `MAC_FLAPPING` + `STP_TOPO_CHANGE` 两类（简化）
- **V5**：全 4 类 + 可扩展

### D8 · BPDU 收发异常（置信度：中，仅 V5）
- **判据**：接口 BPDU 收/发计数异常（收发失衡、TCN 激增）；或非边缘口**收到自己发出的 BPDU**（自环强证据）。
- **阈值**：`BPDU 收 > 1000/s`（可疑）；**自环 BPDU = 直接高危**
- **误报场景**：下游交换机正常转发 → **缓解**：只在**非边缘口**判定；自环判定需比对本机 bridge ID。
- **V4**：不做

### D9 · 跨设备关联（置信度：高，仅 V5）★
- **判据 A（MAC 全局冲突）**：同一 MAC **同时出现在两台不同设备的端口上** → 环路强证据。
- **判据 B（同时刻 TC 抖动）**：多台设备在同一采样窗口 TC 同时跳增 → 广播域级环路。
- **判据 C（表项震荡）**：单设备 MAC 表条目数在两轮间**剧烈波动**（±10% 以上）。
- **阈值**：A 命中即高危；B 需 ≥2 台设备同窗口命中；C 波动 > 10%
- **误报场景**：正常双上行链路（ECMP/堆叠）会有同 MAC 多口现象 → **缓解**：排除**聚合口成员**与**堆叠/IRF 成员**之间的"多口"（这需要识别聚合/堆叠配置）。
- **V4**：不做

### D10 · 光模块收发功率（光衰）（置信度：高）
- **判据 A（绝对值）**：收光功率（RxPower）低于阈值；发光功率（TxPower）异常偏高/偏低。
- **判据 B（趋势，更强）**：两轮间收光功率**下降 > 3 dB** → 快速劣化（比绝对值更早预警）。
- **判据 C（模块自报）**：DDM 诊断字段带 `Alarm` / `Warning` 标志。
- **阈值**：RxPower `< -20 dBm`（可疑）/ `< -25 dBm`（高危）；两轮变化 `> 3 dB`（可疑）
- **误报场景**：光模块型号/传输距离不同，正常范围不同（1000BASE-LX 与 -ZX 差很多）→ **缓解**：阈值按模块类型可覆盖；未知类型只报"趋势"（判据 B），不报绝对值。
- **V4/V5**：均支持

### D11 · 温度告警（置信度：高）
- **判据 A**：传感器温度超阈值（`> 65°C` 可疑 / `> 75°C` 高危）。
- **判据 B（趋势）**：两轮间上升 `> 10°C` → 快速升温（风道堵塞/风扇故障征兆）。
- **阈值**：`temp_warn_c: 65` / `temp_high_c: 75` / `temp_rise_c: 10`
- **误报场景**：夏季机房无空调、机型正常温度偏高 → **缓解**：结合**风扇状态**（若有）判断；单报"温度高"不武断判故障。
- **V4/V5**：均支持

### D12 · 电源状态告警（置信度：高）
- **判据 A**：任一电源状态**非 Normal**（`Abnormal` / `Fault` / `Absent` / `NotSupply`）→ 高危。
- **判据 B（冗余丢失）**：双电源设备两轮间**在位电源数减少**（2 → 1）→ 高危（冗余已失效）。
- **阈值**：状态非 Normal = 直接高危；`power_count` 下降 = 高危
- **误报场景**：几乎无误报（状态字段语义明确）→ 唯一的坑是**字段名/取值各家不同**，需按厂商映射（见 §1）。
- **V4/V5**：均支持
- **备注**：V3 巡检里已有 `display device` 状态列的解析经验（Normal/Abnormal/Fault/Unregistered/NotSupply），可直接复用 ✓

---

## 3. 统一告警模型（输出结构）

每条告警 = 一个结构体（表格一行 + 详情面板）：

| 字段 | 说明 |
|---|---|
| `signal` | 统一信号名（如 `MAC_FLAPPING`） |
| `severity` | `high` / `medium` / `low` → UI 红/黄/绿 |
| `device` / `interface` | 定位（跨设备告警含多设备） |
| `confidence` | 高/中/低（映射可靠性） |
| `evidence` | **原始输出行片段**（必带！便于人工确认） |
| `delta` / `threshold` | 实测增量 vs 阈值（可解释性） |
| `advice` | 建议动作（如"检查端口 X 与 Y 是否形成二层环路"） |
| `rule_id` | 命中的规则编号（D1~D9） |

> 设计底线：**宁可报"可疑 + 带证据"，绝不武断下结论**。

---

## 4. 可调参数（配置文件 / UI 设定区）

```yaml
# rules.yaml —— 唯一在两个工具间共享的文件（纯数据）
version: 1
sampling:
  interval_seconds: 45          # 在线模式两轮间隔
thresholds:
  tc_per_minute: { access: 5, aggregation: 15, core: 30 }
  broadcast_pps: 1000
  broadcast_bandwidth_pct: 5
  crc_per_minute: { warn: 100, high: 1000 }
  link_flap_per_hour: { warn: 3, high: 10 }
  mac_move_count: { warn: 1, high: 2 }
  mac_table_swing_pct: 10
  # 光衰 / 温度 / 电源（D10-D12）
  rx_power_dbm: { warn: -20, high: -25 }      # 收光功率低于此值告警
  rx_power_drop_db: 3                          # 两轮间下降超过此值 → 快速劣化
  temp_c: { warn: 65, high: 75 }               # 传感器温度
  temp_rise_c: 10                              # 两轮间升温超过此值
  power_abnormal_states: ["Abnormal", "Fault", "Absent", "NotSupply", "Unregistered"]
device_classes:                  # 每台设备可归入一类，决定阈值档
  "192.0.2.1": core
  "192.0.2.1": access
signals:                         # 关键字映射（用户可自行扩充）
  MAC_FLAPPING:
    severity: high
    patterns:
      huawei: ["MFLPVLAN", "MAC.{0,20}flapping"]
      h3c:    ["MAC_MOVE", "mac-move"]
      ruijie: ["MACFLAP", "FLAPPING"]
      cisco:  ["MACFLAP_NOTIF", "MAC.{0,20}flapping"]
  STP_TOPO_CHANGE:
    severity: medium
    patterns:
      huawei: ["STP/\\d/TC", "topology.{0,20}change"]
      h3c:    ["STP_TOPO_CHANGE"]
      ruijie: ["SPANTREE-6"]
      cisco:  ["SPANTREE-2", "SPANTREE-6"]
  BROADCAST_STORM:
    severity: high
    patterns: { ".*": ["broadcast.{0,20}storm", "storm.{0,10}control"] }
  LOOP_DETECTED:
    severity: high
    patterns: { ".*": ["loop.{0,10}detect", "LOOP_BACK"] }
```

**规则表缺失时自动释放默认版**（用户无感）；匹配一律**大小写不敏感**；`".*"` = 全厂商通用。

---

## 5. 实现分期

| 阶段 | 内容 | 产物 | 可验证 |
|---|---|---|---|
| **P1** | 判据引擎核心（D1~D6 + **D10~D12**，纯函数） | `ring_rules.py`（V4/V5 各带一份） | 用**模拟输出**跑通判据 |
| **P2** | 离线解析 + 结果展示 | V4 可用（读巡检输出 → 可疑清单） | 用真实巡检文件验证 |
| **P3** | 在线两轮扫描（复用现有批量巡检） | V5 在线模式 | 实验室设备实测 |
| **P4** | D7 关键字映射 + D8 BPDU | V5 完整检测项 | 构造告警日志验证 |
| **P5** | D9 跨设备关联 + 证据链 | V5 深度检测 | 多设备实测（含聚合/堆叠排除） |

---

## 6. 待实测清单（上真机核对）

- [ ] 华为 `display mac-address flapping record` 是否存在、字段格式
- [ ] 华三 `display mac-address mac-move` 字段格式
- [ ] 锐捷 `show mac-address-table flapping` 是否存在
- [ ] 思科 `show spanning-tree detail` 的 `Number of topology changes` 字段名
- [ ] 各家 `display stp` / `show spanning-tree` 的 TC 计数字段名与单位
- [ ] 分页命令（华为 `screen-length 0 temporary` / 思科 `terminal length 0`）在各家的实际效果

---

*本清单为设计规格，尚未实现；实现前请先确认 §1 命令表与 §6 待实测项。*
